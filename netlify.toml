[build]
  command = "npm ci && npm run build"
  publish = "dist"

[functions]
  directory = "dist"
  included_files = ["dist/**/*.js"] 

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/main"
  status = 200
